spring:
  application:
    name: eolmago
  profiles:
    active: ${SPRING_PROFILE:dev}

server:
  port: ${PORT:8080}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html